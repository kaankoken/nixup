# Nix
result
result-*
.direnv/
.pre-commit-config.yaml.bak

# Rust
/target/
**/*.rs.bk

# Editor / OS
.DS_Store
*.swp
*~

# Local secrets and generated agent state (never commit)
*.pem
.env
.env.*
/.tokensave/
/.beads/
.beads/hook-state/
.claude/
.headroom/
.serena/

# Personal nixup config (template: nixup.toml.example)
/nixup.toml

# Local design / plan docs (not for public repo)
/docs/superpowers/
/docs/plans/
/docs/plans/**

# Personal / generated host inventory + modules from `nixup hosts sync`.
# Only the example inventory and example hosts stay tracked for CI/third parties.
/hosts/inventory.nix
/hosts/*
!/hosts/inventory.example.nix
!/hosts/my-mac/
!/hosts/my-mac/**
!/hosts/my-linux/
!/hosts/my-linux/**
